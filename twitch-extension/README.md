# 🎮 Twitch Extension - فوازير روز

## نظرة عامة

امتداد Twitch يسمح للمشاهدين بالانضمام للعبة مباشرة من الشات بكتابة "join" ويتفاعلون مع اللاعب الرئيسي (المذيع) في الألعاب التفاعلية.

---

## 🚀 المميزات

### للمشاهدين (Viewers)
- ✅ انضمام سهل بكتابة "join" في الشات
- ✅ عرض مباشر لحالة اللعبة
- ✅ متابعة النقاط والإحصائيات
- ✅ رؤية قائمة اللاعبين المتصلين
- ✅ واجهة عربية سلسة

### للمذيع (Broadcaster)
- ✅ لوحة تحكم شاملة
- ✅ إدارة اللاعبين والمشاهدين
- ✅ بدء/إيقاف اللعبة
- ✅ اختيار نوع اللعبة
- ✅ تتبع الإحصائيات الحية

---

## 📁 هيكل المشروع

```
twitch-extension/
├── manifest.json                 # إعدادات الامتداد
├── server.js                     # خادم WebSocket
└── src/
    ├── viewer/
    │   └── viewer.html          # صفحة المشاهد
    └── config/
        └── config.html          # لوحة تحكم المذيع

src/app/
├── api/
│   └── twitch/
│       └── route.ts             # API للعبة و Twitch
└── components/
    └── TwitchExtensionIntegration.tsx  # مكون التكامل
```

---

## ⚙️ الإعدادات

### 1. تثبيت الامتداد على Twitch

```bash
# تسجيل الامتداد
twitch ext create

# تحديث الإعدادات
twitch ext update --private-key your-private-key.pem

# النشر
twitch ext release
```

### 2. متطلبات Twitch

- **OAuth Scopes المطلوبة:**
  - `bits:read` - قراءة البتات
  - `channel:read:subscriptions` - قراءة الاشتراكات
  - `chat:read` - قراءة الشات
  - `user:read:email` - قراءة البريد الإلكتروني

---

## 🔌 كيفية العمل

### للمشاهدين

1. **الانضمام للعبة:**
   ```
   في الشات → اكتب: join
   ```

2. **ماذا يحدث؟**
   - يتم إضافة المشاهد تلقائياً لقائمة اللاعبين
   - يحصل على رقم لاعب فريد
   - يرى اسمه في قائمة اللاعبين

3. **متابعة اللعبة:**
   - شاشة الامتداد تعرض حالة اللعبة الحية
   - تحديث النقاط تلقائياً
   - إزالة تلقائية عند الاستبعاد

### للمذيع

1. **البدء:**
   - فتح لوحة التحكم
   - اختيار اللعبة المطلوبة
   - تعيين إعدادات اللعبة

2. **التحكم:**
   - رؤية جميع اللاعبين المتصلين
   - تحديث النقاط يدويّاً
   - استبعاد اللاعبين

---

## 📡 التواصل بين النظام

### WebSocket Events

```javascript
// المشاهد يانضم
{
  type: 'CONNECT',
  userId: 'twitch-user-id',
  displayName: 'المشاهد'
}

// تحديث حالة اللعبة
{
  type: 'GAME_STATE_UPDATED',
  currentGame: 'roulette',
  isActive: true
}

// تحديث النقاط
{
  type: 'UPDATE_SCORE',
  playerId: 'user-id',
  score: 100
}

// استبعاد لاعب
{
  type: 'ELIMINATE_PLAYER',
  playerId: 'user-id'
}
```

---

## 🛠️ API Endpoints

### GET `/api/twitch`
الحصول على قائمة اللاعبين

```bash
GET /api/twitch?action=get-players&gameId=roulette
```

**الرد:**
```json
{
  "success": true,
  "players": [
    {
      "id": "user-id",
      "name": "اللاعب",
      "score": 100,
      "eliminated": false
    }
  ],
  "count": 1
}
```

### POST `/api/twitch`
تحديث حالة اللاعب

```bash
POST /api/twitch
Content-Type: application/json

{
  "action": "join",
  "playerId": "user-id",
  "displayName": "الاسم",
  "gameId": "roulette"
}
```

---

## 🎯 حالات الاستخدام

### سيناريو 1: لعبة روليت مع المشاهدين

1. المذيع يختار لعبة "روليت"
2. يبدأ اللعبة من لوحة التحكم
3. المشاهدون يكتبون "join" في الشات
4. يظهرون تلقائياً في قائمة اللاعبين
5. المذيع يدير اللعبة والنقاط
6. عند انتهاء اللعبة، يتم إعلان الفائز

### سيناريو 2: جولة كراسي موسيقية

1. المذيع يختار لعبة "جولة كراسي"
2. المشاهدون ينضمون عبر "join"
3. الموسيقى تشتغل من موقع فوازير روز
4. المشاهدون يمشون (محاكاة)
5. عند الإيقاف، يتم استبعاد لاعب عشوائي
6. يستمر حتى يبقى لاعب واحد

---

## 🔐 الأمان

- ✅ OAuth 2.0 للمصادقة
- ✅ JWT للتحقق من الهوية
- ✅ HTTPS للاتصالات
- ✅ معالجة آمنة للبيانات الحساسة
- ✅ عدم تخزين كلمات المرور

---

## 🐛 استكشاف الأخطاء

### المشاهد لا يظهر في القائمة
```
✓ تأكد من اتصالك بـ Twitch
✓ تحقق من كتابة "join" بشكل صحيح
✓ حاول تحديث الصفحة
```

### لا توجد اتصالات WebSocket
```
✓ تحقق من أن الخادم يعمل على المنفذ 3001
✓ تحقق من جدار الحماية
✓ شاهد سجلات الأخطاء في Developer Tools
```

### الامتداد لا يحمل
```
✓ استخدم HTTPS في الإنتاج
✓ تحقق من manifest.json
✓ تأكد من تسجيل الامتداد على Twitch
```

---

## 📚 المراجع

- [Twitch Extensions Documentation](https://dev.twitch.tv/docs/extensions)
- [Twitch OAuth](https://dev.twitch.tv/docs/authentication)
- [WebSocket API](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket)

---

## 💡 الخطوات التالية

- [ ] دعم الفيديو المباشر
- [ ] نظام الرهانات والنقاط
- [ ] التسجيل والإحصائيات
- [ ] دعم لغات متعددة
- [ ] نظام الإشارات الصوتية المخصصة

---

## 📝 الترخيص

جميع الحقوق محفوظة © 2024 فوازير روز
